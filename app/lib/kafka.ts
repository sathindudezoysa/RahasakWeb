type message ={
    documentId: string;
    msg: string;
    messageType: string;
    recipientId: string;
    senderId: string;
    staus: string;
    timestamp: string;
}

export async function sendMessage(username: string, userkey: string, friendkey: string){
    
}